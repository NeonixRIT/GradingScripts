# List all repos matching CSV or All and let them select number range(inclusive), number by comma, or all
# Option to extract selections to CSV
# Clone, Add, Rollback option for each

# Compact summary of a repo? include name, commits, cloned status
